import React from 'react';

const Feature = ({feature:{image, name, number, address, rating}}) => {
    console.log(image)
    return (
        <div>
            
        </div>
    );
};

export default Feature;